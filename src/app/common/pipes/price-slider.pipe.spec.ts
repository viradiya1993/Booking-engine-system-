import { PriceSliderPipe } from './price-slider.pipe';

describe('PriceSliderPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSliderPipe();
    expect(pipe).toBeTruthy();
  });
});
