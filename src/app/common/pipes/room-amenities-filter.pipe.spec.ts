import { RoomAmenitiesFilterPipe } from "./room-amenities-filter.pipe";

describe("RoomAmenitiesFilterPipe", () => {
  it("create an instance", () => {
    const pipe = new RoomAmenitiesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
