import { RoomsSortByPipe } from "./rooms-sort-by.pipe";

describe("RoomsSortByPipe", () => {
  it("create an instance", () => {
    const pipe = new RoomsSortByPipe();
    expect(pipe).toBeTruthy();
  });
});
