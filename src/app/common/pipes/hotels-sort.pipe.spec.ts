import { HotelsSortPipe } from "./hotels-sort.pipe";

describe("HotelsSortPipe", () => {
  it("create an instance", () => {
    const pipe = new HotelsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
